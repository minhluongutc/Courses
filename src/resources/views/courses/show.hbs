<style>
    
.btn-text {
    background-color: #f47425;
    width: 100%;
    display: flex;
    justify-content: center;
    padding: 11.7px;
    border: 1px solid hsl(27, 90%, 55%);
    text-decoration: none;
    font-size: 20px;
    color: #fff;
    cursor: pointer;
    transition-duration: .7s;
    font-weight: bold;
}

.btn-text:hover {
    background-color: #000;
    color: #f47425;
    border: 1px solid hsl(27, 90%, 55%);
    transition-duration: .7s;
}

.btn-text-new:hover::before {
    content: attr(data-price);
}
.btn-text-new::before {
    content: "Mua khóa học";
}

</style>


<div class="row">
    <div class="col-lg-12">
        <iframe style="width: 100%; height: 30.25vw" src="https://www.youtube.com/embed/{{course.videoId}}" title="YouTube video player" frameborder="0" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share" allowfullscreen></iframe>
        <h2 style="padding-bottom: 17px;">TRAILER khóa học {{course.name}}</h2>
        <div class="row" style="padding-bottom: 50px;">
            <div class="col-lg-9 col-md-12 col-sm-12">
                <h3>{{course.description}}</h3>
            </div>
            <div class="col-lg-3 col-md-12 col-sm-12">
                <form method="POST" action="/banking/generateForCourse" >
                    
                    <input value="Thanh toán khóa học {{course.name}}" type="text" class="input-email  c-12 m-12 l-12" name="addInfo" id="addInfo" style="display: none;">
                    <input value="{{course.price}}" type="text" class="input-email  c-12 m-12 l-12" name="amount" id="amount" style="display: none;">
                    <button style="text-decoration: none;"class="btn-text btn-text-new" data-price="{{course.price}} vnđ"></button>
                </form>
            </div>
        </div>
    </div>
</div>
